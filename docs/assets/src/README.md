# Sources for the rendered assets

`social-preview.html` renders to `../social-preview.png` (1280 by 640) in a headless
browser; it is the image set in the repository's social preview. `unicorn-41x42.*`
is the pixel grid the banner and the social preview draw the mascot from. Nothing
here is read by the binary.
